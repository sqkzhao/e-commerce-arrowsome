import { makeStyles } from '@material-ui/core';

export default makeStyles((theme) => ({
    root: {
        flexGrow: 1,
        paddingTop: '25px',
    },
    menuButton: {
        // marginRight: theme.spacing(2),
        flexGrow: 1,
    },
    title: {
        flexGrow: 1,
    },
}));